# 任务配置、计划与提醒

## 当前交付

代码和隔离 PostgreSQL 测试已经覆盖 PIN 会话授权、周期配置、计划结算与提醒。尚未修改线上 Supabase，Android/iOS 通知权限、后台触发和重启恢复仍需真机验收。原 PRD 未修改、未加入本次 Git 提交。

## PIN 授权边界

- 验证成功后授权绑定家庭、用户和 Supabase JWT 的 session_id，有效期 10 分钟。
- 管理 RPC 和内容写入 RLS 在服务端检查授权；本地 bool 不能代替服务端授权。
- 家庭邀请详情和审计日志的读取同样需要有效 PIN 授权。
- 退出家长模式、切换孩子、退出登录时撤销当前会话授权；网络失败会提示重试，不宣称已撤销。
- 首次设置 PIN 要求管理员在登录后的 5 分钟内操作；设置后仍需验证才能获得授权。
- 更换 PIN 会撤销其他会话的授权；连续五次失败锁定十分钟。
- 普通家长可以在有效授权内配置任务和计划；补签仍保留管理员限制。扩大补签权限的额外调整被自动审批拦截，未实施。
- 这不是独立孩子登录身份。当前家庭成员仍能读取家庭内孩子的数据；独立儿童账户和更细的读取隐私不在本次交付中。

## 使用入口

首页“任务管理” → 右上角日程按钮 → “周期模板、长期计划与提醒”。家长授权有效时可新建配置；未授权或到期后为只读。

支持一次性、每天、每周指定星期、自定义日期，以及开始/结束日期、家庭时区内的开始/截止时间、单次金币/XP和最低有效分钟数。每周未选择的星期为休息日。最长配置跨度 366 天。编辑只影响后续实例，已生成的任务保存原规则快照；模板可启停。

长期计划创建后锁定规则，支持批准当日及未来未开始任务的请假。完成率分母包括尚未生成的应完成日期，排除批准请假日；漏开应用不会降低分母。阶段奖励按完成次数发放一次，到期终奖只取达到的最高完成率档，不累计各档。

阶段奖励输入示例：`3:20:30,7:50:50`，含义为完成 3 次奖励 20 金币/30 XP，完成 7 次奖励 50/50。更高终奖档位示例：`90:200:200,100:300:300`；百分比必须高于基础达标率。

## 提醒

提醒设置内可以关闭提醒、设置静默时段，并显式申请 Android/iOS 系统通知权限。服务端按家庭时区计算开始前 10 分钟、开始时、截止前 15 分钟三个候选时点，相同时间合并，静默时段过滤，已读不再返回。

预计算未来七天提醒，不提前生成未来可领奖任务。设备调度最近 60 个未来时点；打开应用、回到前台、任务或提醒刷新时同步。完成/跳过/已读和设置关闭后取消对应待调度通知；切换孩子和账号清理旧通知。锁屏正文不显示孩子昵称、任务题目等信息。Android 使用非精确闹钟，不申请精确闹钟特殊权限。

当前为本地通知而非远程推送。另一台设备修改任务时，当前设备在下次同步前仍可能显示旧提醒；离线期间的新计划不能自动到达本机。操作系统节电策略可能延迟通知，需要真机验证。

## 部署与定时作业

1. 在目标开发/staging环境审查并应用迁移 005–008。先核对连接目标；不要用 db reset 操作线上数据。
2. 迁移 008 在 pg_cron 已启用时注册每五分钟维护作业：按家庭当天幂等生成任务并结算已结束计划。
3. 若迁移时尚未启用 Cron，审查后在目标 Supabase 环境运行 `supabase/operations/enable_task_maintenance.sql`。本次没有执行该线上操作。
4. 检查 cron.job 与 cron.job_run_details，确认作业注册和执行结果。
5. 系统维护函数只允许数据库调度者或 service_role 调用，普通登录用户不能伪造系统上下文。

## 验证

当前已通过 24 项数据库检查和 28 项客户端单元/Widget 测试；静态分析以最后一次执行结果为准。

- `npm run test:db`：新建独立临时 PostgreSQL，模拟 Supabase auth 函数/JWT，执行全部真实迁移和 RLS/RPC回归；不连接已链接的 Supabase项目。
- Windows 中文二进制路径下 PostgreSQL 无法初始化，验证脚本将测试运行时复制到临时英文路径；测试结束停止该服务。
- CI 新增独立数据库回归任务；Flutter质量检查和 Android/iOS构建任务保留。
- `dart analyze`、`flutter test --no-test-assets`：本机 FlutterSDK 中文路径存在测试 shader 编译问题，关闭测试 assets以绕开；不是生产功能开关。
- 尚未完成：真实 Supabase Auth跨设备集成、线上Cron运行验收、Android/iOS真机通知、离线打卡/同步队列。

通知平台配置依据：[插件官方文档](https://pub.dev/packages/flutter_local_notifications)。独立数据库运行时：[embedded-postgres官方说明](https://github.com/leinelissen/embedded-postgres)。
